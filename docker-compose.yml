version: "3.9"
services:
  creatovetter:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
      - ./.env:/app/.env
    env_file:
      - .env
